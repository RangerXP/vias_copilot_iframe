{
  "name": "pbie-context-agent",
  "version": "1.0.0",
  "description": "VISA Commercial Spend Analytics — PBIE + FilterSession Context Injection (Pattern 2)",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@azure/identity": "^4.4.0",
    "@azure/msal-node": "^2.15.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.19.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
